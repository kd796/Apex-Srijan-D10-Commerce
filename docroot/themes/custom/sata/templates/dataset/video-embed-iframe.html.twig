{#
/**
 * @file
 * Display an iframe with alterable components.
 */
#}
<iframe{{ attributes }} src="{{ url }}" {% if query is not empty %}?{{ query | url_encode }}{% endif %}{% if fragment is not empty %}#{{ fragment }}{% endif %}"></iframe>
