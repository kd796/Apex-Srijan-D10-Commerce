{#
/**
 * @file
 * Template for the wishlist user form.
 *
 * Available variables:
 * - form: The form.
 *
 * @ingroup themeable
 */
#}
<div class="wishlist-user-form clearfix">
  <div class="wishlist__heading"> {{'My Wishlist'|t }} </div>
  <div class="wishlist-header">
    <div class="item-heading">
      {{ 'Product/Item'|t }}
    </div>
    <div class="item-quantity"> {{ 'Quantity'|t }} </div>
    {{ form.header }}
  </div>
  {# {{ vardumper(form.items) }} #}
  <div class="wishlist-items">
    {% for key, item in form.items %}
      {% if key|first != '#' %}
        <div class="wishlist-item">
          <div class="wishlist-item__entity">
            {{ item.entity }}
          </div>
          <div class="wishlist-item__details">
            {{ item.details }}
          </div>
          <div class="wishlist-item__actions">
            {{ item.actions }}
          </div>
        </div>
      {% endif %}
    {% endfor %}
  </div>
  {{ form|without('header', 'items') }}
</div>
