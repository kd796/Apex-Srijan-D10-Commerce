{% set centered = elements.field_center[0]['#markup'] | trim | lower is same as('true') %}
{% set show_title = elements.field_show_title[0]['#markup'] | trim | lower is same as('true') %}
{{ pass_attributes(elements.field_callout, {
	inverse: true,
	centered: centered,
	size: 'large',
	show_title: show_title
}) }}
