view.webform_submission.all:
  path: '/admin/structure/webform/manage/{webform}/results/submissions'
  defaults:
    _title: 'Webform Results'
  requirements:
    _permission: 'administer webform customer support'